# BUMDes Karang Baru — Vite + React (Tailwind-ready)

Project generated for *BUMDes Karang Baru - Wanasaba, Lombok Timur*.

## Quick start

1. Install dependencies:
   ```bash
   npm install
   ```

2. Run development server:
   ```bash
   npm run dev
   ```

3. Build for production:
   ```bash
   npm run build
   ```

Note: This template includes Tailwind configuration files so you can enable Tailwind by running `npm install` first. If you don't want Tailwind, remove the PostCSS/Tailwind files and index.css directives.
